# App Store manifests — Phase 1 placeholder

No application manifests ship in Phase 1 (no Immich installation).
The manifest format (`manifestVersion: 1`, validation via
`@serveros/contracts`) lands with Phase 6 (App Manifest system).

Directory layout (reserved):

```text
manifests/official/<app-id>/manifest.yml
manifests/official/<app-id>/icon.png
manifests/community/...
```
